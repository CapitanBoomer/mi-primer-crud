import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CRUD cubitos MC',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
